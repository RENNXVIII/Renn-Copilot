{
  "name": "renn-copilot",
  "displayName": "Renn Copilot",
  "description": "Self-contained CLIProxyAPI dashboard + BYOK model sync for GitHub Copilot Chat, entirely inside VS Code -- no separate terminal or browser tab needed.",
  "version": "0.4.13",
  "publisher": "RENNXVIII",
  "icon": "media/icon.png",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "rennCopilot.syncModels",
        "title": "Renn Copilot: Sync Models from Dashboard"
      },
      {
        "command": "rennCopilot.copyApiKey",
        "title": "Renn Copilot: Copy API Key to Clipboard"
      },
      {
        "command": "rennCopilot.showHealthDetails",
        "title": "Renn Copilot: Show Provider Account Health"
      },
      {
        "command": "rennCopilot.startBackend",
        "title": "Renn Copilot: Start Backend"
      },
      {
        "command": "rennCopilot.stopBackend",
        "title": "Renn Copilot: Stop Backend"
      },
      {
        "command": "rennCopilot.openDashboardPanel",
        "title": "Renn Copilot: Open Dashboard",
        "icon": "$(dashboard)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "rennCopilot",
          "title": "Renn Copilot",
          "icon": "media/activitybar-icon.svg"
        }
      ]
    },
    "views": {
      "rennCopilot": [
        {
          "type": "webview",
          "id": "rennCopilot.sidebarView",
          "name": "Dashboard"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "rennCopilot.openDashboardPanel",
          "when": "view == rennCopilot.sidebarView",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Renn Copilot Bridge",
      "properties": {
        "rennCopilot.backendUrl": {
          "type": "string",
          "default": "http://127.0.0.1:4317",
          "description": "Base URL of the renn-copilot backend (the same one the dashboard talks to)."
        },
        "rennCopilot.autoSyncOnStartup": {
          "type": "boolean",
          "default": true,
          "description": "Automatically sync enabled models into Copilot's BYOK setting when VS Code starts."
        },
        "rennCopilot.autoStartBackend": {
          "type": "boolean",
          "default": true,
          "description": "Automatically start the renn-copilot backend when VS Code starts. Disable to start it manually via \"Renn Copilot: Start Backend\"."
        },
        "rennCopilot.autoStartServer": {
          "type": "boolean",
          "default": true,
          "description": "Automatically start the CLIProxyAPI server once the backend is up (same as clicking \"Start\" on the Overview page). Only takes effect once the binary has been installed at least once; has no effect if rennCopilot.autoStartBackend is disabled."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "vscode:prepublish": "npm run compile",
    "backend:dev": "npm run dev --prefix backend",
    "backend:install": "npm install --omit=dev --prefix backend",
    "webview:dev": "npm run dev --prefix webview-ui",
    "webview:install": "npm install --prefix webview-ui",
    "webview:build": "npm run build --prefix webview-ui",
    "package": "npm run backend:install && npm run webview:install && npm run webview:build && vsce package --allow-missing-repository"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.95.0",
    "typescript": "^5.6.0",
    "@vscode/vsce": "^3.2.1"
  }
}
