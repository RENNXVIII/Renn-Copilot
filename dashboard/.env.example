# Base URL of the renn-copilot backend. Only needed if it's not on the
# default http://127.0.0.1:4317 (e.g. a different PORT in backend/.env, or
# the backend running on another machine on your LAN).
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:4317
